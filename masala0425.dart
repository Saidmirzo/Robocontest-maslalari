import 'dart:io';

void main(List<String> args) {
  List<int> nvam =
      stdin.readLineSync()!.split(' ').map((e) => int.parse(e)).toList();
  List<int> times =
      stdin.readLineSync()!.split(' ').map((e) => int.parse(e)).toList();
  print(1);
  exit(0);
}
