import 'dart:io';

void main(List<String> args) {
  List<int> mvan =
      stdin.readLineSync()!.split(' ').map((e) => int.parse(e)).toList();
  List<int> toshlarSoni =
      stdin.readLineSync()!.split(' ').map((e) => int.parse(e)).toList();
  toshlarSoni.sort();
  int m = mvan[0], n = mvan[1];
  for(int i=n;i>=0;i--)
  {
    if()
    for(int j=n-1;j>=0;j++)
    {
      if()
    }
  }
}
